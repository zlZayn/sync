# 工作日志

## 日期：2026-02-28

## 标题：防跌倒扶手调研数据聚类分析与数据格式统一

### 一、目标
进行防跌倒扶手调研数据的聚类分析（K-means和分层聚类），并统一子女和老年人群体的变量格式。

### 二、内容

1. **数据格式统一**
   - 将老年人`扶手偏好类型_2_2_E`转换为独热编码，与子女群体变量格式保持一致

2. **K-means聚类分析**
   - 筛选预处理变量，标准化处理
   - 用轮廓系数法确定最佳K值并执行聚类
   - 生成特征剖面，导出聚类标签为CSV

3. **分层聚类分析**
   - 采用与K-means相同的变量处理
   - 用欧氏距离和Ward方法执行聚类
   - 确定最佳聚类数，生成特征剖面并导出结果

4. **聚类特征剖面提取**
   - 提取两种聚类方法的特征剖面数据框

5. **聚类结果分析**
   - 标准化特征剖面数据，计算聚类间差异
   - 可视化高区分度特征，展示聚类效果

### 三、结果

1. **数据格式统一**
   - 成功转换老年人变量为独热编码，与子女群体格式一致

2. **聚类分析结果**
   - **K-means聚类**：自动确定最佳聚类数，生成特征剖面和标签
     - 导出文件：`output/子女_K-means聚类标签.csv`、`output/老年人_K-means聚类标签.csv`
   
   - **分层聚类**：使用Ward方法，自动确定最佳聚类数
     - 导出文件：`output/子女_分层聚类标签.csv`、`output/老年人_分层聚类标签.csv`

3. **特征剖面数据**
   - 生成四个特征剖面数据框：
     - `df_C_kmeans_profile`、`df_E_kmeans_profile`
     - `df_C_hierarchical_profile`、`df_E_hierarchical_profile`
   - 保存为：`data/cluster_profiles.RData`

4. **聚类效果评估**
   - 生成轮廓系数图表评估聚类质量
   - 可视化结果，识别高区分度特征

5. **新增文件**
   - `code/CA_分层.qmd`、`code/CA_K-means.qmd`
   - `code/extract_cluster_profile_df.qmd`、`code/cluster_profile_df_analysis.qmd`

### 四、总结

完成防跌倒扶手调研数据的聚类分析（K-means和分层聚类），统一了子女和老年人群体的变量格式，生成了详细的特征剖面。

通过自动确定最佳聚类数、生成特征剖面和可视化分析，为后续研究提供了重要支持。产出的聚类标签和特征剖面数据可用于进一步分析，为防跌倒扶手的推广和优化提供科学依据。

下一步将基于聚类结果，深入分析不同群体的需求差异，为防跌倒扶手的设计和推广策略提供针对性建议。